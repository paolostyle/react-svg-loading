export interface GeoAttributes {
  x: number;
  y: number;
  width: number;
  height: number;
}

export interface Direction {
  x: number;
  y: number;
  trX: number;
  trY: number;
}

export interface RootStyle {
  width: string;
  height: string;
}
